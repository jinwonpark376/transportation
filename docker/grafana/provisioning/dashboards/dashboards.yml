# Grafana 대시보드 자동 프로비저닝
apiVersion: 1

providers:
  - name: 'Transportation Dashboards'
    orgId: 1
    folder: 'Transportation'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
